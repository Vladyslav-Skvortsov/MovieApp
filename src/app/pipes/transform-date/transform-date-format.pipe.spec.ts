import { TransformDateFormatPipe } from './transform-date-format.pipe';

describe('TransformDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
